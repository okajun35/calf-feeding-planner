{
  "version": "v1",
  "hooks": [
    {
      "name": "Lint on Save",
      "trigger": "PostFileSave",
      "description": "src/ または tests/ の .js ファイル、もしくは style.css を保存するたびに ESLint + Stylelint を自動実行する",
      "matcher": "\\.(js|css)$",
      "action": {
        "type": "command",
        "command": "npm run lint 2>&1 || true"
      }
    }
  ]
}